export interface Founder {
  name: string;
  role: string;
  image: string;
}

export interface Capability {
  heading: string;
  body: string;
}

export interface Product {
  name: string;
  body: string;
  href: string;
  buttonText: string;
  screenshot: string;
}
